<Application x:Class="PhysioCenter.Wpf.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

        <Application.Resources>
                <!-- ألوان عامة -->
                <Color x:Key="PrimaryBlueColor">#FF1C84FF</Color>
                <Color x:Key="PrimaryGreenColor">#FF2ECC71</Color>
                <Color x:Key="DarkBgColor">#FF050913</Color>
                <Color x:Key="CardBgColor">#80121A2E</Color>

                <SolidColorBrush x:Key="PrimaryBlueBrush"
                                 Color="{StaticResource PrimaryBlueColor}"/>
                <SolidColorBrush x:Key="PrimaryGreenBrush"
                                 Color="{StaticResource PrimaryGreenColor}"/>
                <SolidColorBrush x:Key="DarkBgBrush"
                                 Color="{StaticResource DarkBgColor}"/>
                <SolidColorBrush x:Key="CardBgBrush"
                                 Color="{StaticResource CardBgColor}"/>
                <SolidColorBrush x:Key="WhiteBrush"
                                 Color="White"/>

                <!-- بطاقة زجاجية -->
                <Style x:Key="GlassCardStyle"
                       TargetType="Border">
                        <Setter Property="CornerRadius"
                                        Value="30"/>
                        <Setter Property="Background"
                                        Value="#40FFFFFF"/>
                        <Setter Property="BorderBrush"
                                        Value="#60FFFFFF"/>
                        <Setter Property="BorderThickness"
                                        Value="1"/>
                        <Setter Property="Padding"
                                        Value="24"/>
                        <Setter Property="Effect">
                                <Setter.Value>
                                        <DropShadowEffect BlurRadius="30"
                                                          Color="Black"
                                                          ShadowDepth="0"
                                                          Opacity="0.3"/>
                                </Setter.Value>
                        </Setter>
                </Style>

                <!-- زر رئيسي -->
                <Style x:Key="PrimaryButtonStyle"
                       TargetType="Button">
                        <Setter Property="Foreground"
                                        Value="White"/>
                        <Setter Property="FontWeight"
                                        Value="SemiBold"/>
                        <Setter Property="Padding"
                                        Value="10,6"/>
                        <Setter Property="Cursor"
                                        Value="Hand"/>
                        <Setter Property="BorderThickness"
                                        Value="0"/>
                        <Setter Property="Template">
                                <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                                <Border CornerRadius="15">
                                                        <Border.Background>
                                                                <LinearGradientBrush StartPoint="0,0"
                                                                                     EndPoint="1,1">
                                                                        <GradientStop Offset="0"
                                                                                      Color="{StaticResource PrimaryGreenColor}"/>
                                                                        <GradientStop Offset="1"
                                                                                      Color="{StaticResource PrimaryBlueColor}"/>
                                                                </LinearGradientBrush>
                                                        </Border.Background>
                                                        <ContentPresenter HorizontalAlignment="Center"
                                                                          VerticalAlignment="Center"
                                                                          Margin="10,4"/>
                                                </Border>
                                        </ControlTemplate>
                                </Setter.Value>
                        </Setter>
                </Style>

        </Application.Resources>
</Application>
