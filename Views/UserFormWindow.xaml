<Window x:Class="PhysioCenter.Wpf.Views.UserFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="إدارة مستخدم"
        Height="350"
        Width="420"
        WindowStartupLocation="CenterScreen"
        Background="#FF1A1F2A"
        FlowDirection="RightToLeft"
        ResizeMode="NoResize">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText"
                   Text="إضافة مستخدم"
                   FontSize="22"
                   Foreground="White"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1"
                Text="اسم المستخدم"
                Foreground="White"/>
        <TextBox Grid.Row="1"
                 x:Name="UsernameBox"
                 Margin="0,20,0,0"/>

        <TextBlock Grid.Row="2"
                Text="كلمة المرور"
                Foreground="White"
                Margin="0,20,0,0"/>
        <PasswordBox Grid.Row="2"
                     x:Name="PasswordBox"
                     Margin="0,50,0,0"/>

        <TextBlock Grid.Row="3"
                Text="الدور"
                Foreground="White"
                Margin="0,20,0,0"/>
        <ComboBox Grid.Row="3"
                  x:Name="RoleCombo"
                  Margin="0,50,0,0">
            <ComboBoxItem Content="Admin"/>
            <ComboBoxItem Content="Therapist"/>
            <ComboBoxItem Content="Receptionist"/>
        </ComboBox>

        <Button Grid.Row="4"
                Content="حفظ"
                Height="40"
                Margin="0,20,0,0"
                Style="{StaticResource PrimaryButtonStyle}"
                Click="Save_Click"/>
    </Grid>

</Window>
