<UserControl x:Class="PhysioCenter.Wpf.Views.PatientsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- العنوان + زر الإضافة -->
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="المراجعين"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="White"/>

            <Button Content="إضافة مراجع"
                    Margin="20,0,0,0"
                    Padding="20,8"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="AddPatient_Click"/>
        </StackPanel>

        <!-- جدول المرضى -->
        <Border Grid.Row="1"
                Style="{StaticResource GlassCardStyle}"
                Margin="0,20,0,0">

            <DataGrid x:Name="PatientsGrid"
                      AutoGenerateColumns="False"
                      Background="#22FFFFFF"
                      Foreground="White"
                      HeadersVisibility="Column"
                      IsReadOnly="True">

                <DataGrid.Columns>

                    <DataGridTextColumn Header="الاسم"
                                        Binding="{Binding FullName}"
                                        Width="*"/>

                    <DataGridTextColumn Header="الهاتف"
                                        Binding="{Binding Phone}"
                                        Width="150"/>

                    <DataGridTextColumn Header="تاريخ الميلاد"
                                        Binding="{Binding BirthDate}"
                                        Width="150"/>

                    <DataGridTextColumn Header="الملاحظات"
                                        Binding="{Binding Notes}"
                                        Width="*"/>

                </DataGrid.Columns>

            </DataGrid>

        </Border>

    </Grid>

</UserControl>
