<UserControl x:Class="PhysioCenter.Wpf.Views.TherapistsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- العنوان + زر الإضافة -->
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="المعالجون"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="White"/>

            <Button Content="إضافة معالج"
                    Margin="20,0,0,0"
                    Padding="20,8"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="AddTherapist_Click"/>
        </StackPanel>

        <!-- جدول المعالجين -->
        <Border Grid.Row="1"
                Style="{StaticResource GlassCardStyle}"
                Margin="0,20,0,0">

            <DataGrid x:Name="TherapistsGrid"
                      AutoGenerateColumns="False"
                      Background="#22FFFFFF"
                      Foreground="White"
                      HeadersVisibility="Column"
                      IsReadOnly="True">

                <DataGrid.Columns>

                    <DataGridTextColumn Header="الاسم"
                                        Binding="{Binding FullName}"
                                        Width="*"/>

                    <DataGridTextColumn Header="التخصص"
                                        Binding="{Binding Specialty}"
                                        Width="*"/>

                </DataGrid.Columns>

            </DataGrid>

        </Border>

    </Grid>

</UserControl>
